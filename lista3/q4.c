#include <stdio.h>
#include <string.h>
#define TAM 30
int main(void){
    char frase[TAM], frase2[TAM], j[TAM];
    puts("escreva uma frase:");
    fgets(frase, TAM, stdin);
    puts("escreva a segunda frase:");
    fgets(frase2, TAM, stdin);
   for(int i=0; i<TAM; i++){
        if(frase[i] != '\n') 
        j[i]= frase[i];
    }
    for(int i= 0; i<TAM; i++){

    }
    
}